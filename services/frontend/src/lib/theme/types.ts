export interface ThemePalette {
	bg: string
	bgSecondary: string // cards, inputs
	bgTertiary: string // hover states, lines
	text: string
	textSecondary: string
	textTertiary: string
	primary: string
	primaryText: string
	error: string
	success: string
}

export interface SafeArea {
	top: number
	bottom: number
	left: number
	right: number
}
