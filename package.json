{
  "name": "chopcheck",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "dev": "bun run --filter='./services/*' dev",
    "build": "bun run --filter='./services/* build"
  }
}